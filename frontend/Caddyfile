{
    admin off
}

:{$PORT} {
    # Using relative path from the 'frontend' root directory
    root * ./dist

    file_server

    # Standard routing for React/Vite SPAs
    try_files {path} /index.html
}