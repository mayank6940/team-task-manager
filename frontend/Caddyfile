{
    admin off
}

:{$PORT} {
    # Absolute path inside the Railway container
    root * /app/dist
    
    file_server
    
    # This rule sends index.html if the .js file isn't found
    try_files {path} /index.html
}