{
    admin off
}

:{$PORT} {
    # On Railway, with Root Directory set to 'frontend', 
    # the files are typically at /app/dist or /app/frontend/dist.
    root * /app/dist
    
    file_server
    
    # Crucial: This handles the SPA routing
    try_files {path} /index.html
}