{
    admin off
}

:{$PORT} {
    # Match the root to where the files actually sit
    root * ./dist
    
    # Handle the specific case for assets to avoid MIME errors
    handle /assets/* {
        file_server
    }

    # Standard SPA handling
    handle {
        file_server
        try_files {path} /index.html
    }
}